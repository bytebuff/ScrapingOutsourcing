****************2019-05-01******************
[项目难点]
    1. 抓包抓到的是Json格式 但是其实是数组  是JS生成的数组  不能使用json.load() 来解析 解决方法是使用execjs.eval(response.text)
    2. 数据的对应起来  虽然数据都在抓包的资源包了里面  但是对应关系要慢慢查看 比如: Match Report是程序中
    3. 第一组中的第14个元素代表的是是否有Report的国家